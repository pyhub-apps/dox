use anyhow::Result;

fn main() -> Result<()> {
    // Temporary scaffold binary; real CLI will be migrated here.
    println!("dox workspace scaffold: CLI will be migrated to crates/dox-cli");
    Ok(())
}
